apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: spa-fallback
  namespace: betterclassroom
spec:
  redirectRegex:
    regex: "^(?!/api)(.*)"
    replacement: "/index.html"
    permanent: false
